import {StyleSheet} from 'react-native';

export const balanceChartStyle = StyleSheet.create({
  title: {
    padding: 20,
    fontWeight: '600',
    color: '#000',
    fontSize: 16,
  },
  chart: {
    marginVertical: 8,
    borderRadius: 16,
  },
});
